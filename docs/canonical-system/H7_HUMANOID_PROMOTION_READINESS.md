# H7 humanoid promotion readiness

Status: **scope audit complete; promotion blocked pending reversible candidate
lifecycle proof**.

H7 asks whether H1-H6 form a coherent canonical promotion candidate. It does
not import an asset, promote anything, infer owner authority, or claim that the
structural wire and approved visual target are already one generated model.

## Candidate alternatives

| Candidate | Evidence fit | Disposition |
|---|---|---|
| Production humanoid asset | No surface geometry, topology, rig, deformation, animation, material, shader, runtime, or device-cost artifact exists. PolyOne preview pixels are a visual target, not an acquired file. | **Rejected.** Promotion would fabricate a product artifact and widen every H1-H6 claim. |
| H3 structural humanoid candidate | Exact 17-joint/16-link identity, replay, structural controls, and recovery are verified. H5 does not observe a rendered H3 surface because H3 has none. | **Rejected as a visually approved humanoid or production candidate.** Retained as one component of the proof baseline. |
| PolyOne marketplace asset | Owner approved the fingerprinted preview direction. The downloadable file, embedded licence bundle, topology, rig, deformation, derivative lineage, and phone cost remain uninspected. | **Rejected as an import or production candidate.** Retained only as H5 visual-direction evidence. |
| H1-H6 engine-neutral humanoid proof baseline v1 | H1 provenance, H2 structure, H3 deterministic generation, H4 calibrated structural controls, H5 owner-bound visual direction, and H6 reconstruction/recovery are all exact and deliberately separate. | **Selected as the only coherent H7 candidate type.** It is a standard for future work, not a humanoid asset. |

The selected candidate's proposed name is
`engine-neutral-humanoid-proof-baseline-v1`. Its evidence root is H6 manifest
`a0eb0796a4a0edd800fcd937049eaa3ed7c65e695531daf0f754e835591ada2d`.
Its H5 decision root is
`5c4eb3041ced04e1c1a5cd0e011babafe1826a4d8caf420bf267c8bff0617520`.

## Claim and non-claim matrix

| Promotion requirement | Retained evidence | Candidate claim | Readiness |
|---|---|---|---|
| Provenance | H1 archive and entry fixity; typed suite; rejected-source limits | Exact recovered and Forge-owned structural sources are traceable and authority-bounded. | Pass |
| Structural representation | H2 profile `c44adba...d6e3`; exact hierarchy, rest rule, coordinates | A deterministic neutral 17-joint structural reference exists. | Pass |
| Functional generation | H3 candidate `4d04df...07fa`; capability-negative replay | The structural candidate rebuilds deterministically from pinned typed inputs. | Pass |
| Calibrated controls | H4 calibration `774a79...f4d`; orthogonal exact integer controls | Three named structural failures are detectable only within the declared synthetic fixture. | Pass |
| Calibrated observation | H5 pixel receipts and owner decision | PolyOne preview SHA-256 `f3242d...5051` is the approved stylized feminine visual target; masculine direction is strong/commanding. This is not observation of H3 geometry. | Pass for proof-baseline direction; fail for any asset candidate |
| Recovery | H6 strict chain, hostile matrix, seven Kernel objects, ProofReceipt, backup/reopen | Exact H1-H5 identities and limitations reconstruct and recover without authority widening. | Pass |
| Candidate scope | This audit and the H6 manifest | The standard binds separately scoped structural and visual evidence for later surface work. | Pass once encoded and adversarially tested |
| Approval and promotion authority | Kernel and explicit-authorization contracts | Only exact, separate, direct-owner actions may change candidate state. | Pass in existing forward path |
| Rollback/supersession | Control-plane rollback exists only for failed/blocked work gates; Kernel code rollback applies only to code application | A promoted canonical baseline can be retired or superseded append-only without erasing history. | **Fail: candidate lifecycle has no such state or event** |

## Mandatory non-claims

Promotion of this proof baseline must not claim or cause:

- a generated or imported surface asset;
- anatomical truth or population-wide body rules;
- topology, UVs, skinning, inverse-bind matrices, deformation, animation,
  physics, materials, shaders, or ageing visuals;
- phone, PC, GPU, memory, bandwidth, or frame-time fitness;
- production readiness, runtime compatibility, engine selection, application,
  publishing, spending, or acquisition;
- gameplay capability, intelligence, morality, social role, importance, or
  human-binary rules for non-human lineages; or
- approval, promotion, rejection, supersession, or protected authority from a
  receipt, assistant output, captured conversation, or ordinary assent.

## Rollback gap and alternatives

The current Kernel candidate lifecycle is `Proposed -> Approved -> Promoted`,
with terminal `Rejected` or `Quarantined` paths. `reject_candidate` accepts only
a `Proposed` candidate. There is no event or state for withdrawing an approved
candidate or superseding a promoted canonical baseline.

| Alternative | Finding |
|---|---|
| Leave the old candidate `Promoted` and promote a correction | Rejected: two apparently canonical baselines remain active and the old status is misleading. |
| Treat a control-plane rollback record as candidate rollback | Rejected: that record is authority-negative and applies to failed/blocked work gates, not Kernel candidate state. |
| Change documentation only | Rejected: generated or authored projections cannot override the protected ledger. |
| Append an explicit owner-authorized candidate-supersession event and state | Selected for design. It preserves history, makes the prior baseline inactive, binds correction evidence and an optional replacement, and has no application/runtime effect. |

The pre-H7 rollback target is **no promoted humanoid proof baseline**, while the
verified H6 manifest remains immutable evidence. After any later replacement,
the rollback/supersession record must identify both old and new candidate IDs
and retain the old evidence rather than deleting it.

## Cheapest proof before Kernel work

Before any protected-Kernel change, a capability-free typed fixture must prove:

1. the narrow candidate package binds the exact H6/H5 IDs, claims, non-claims,
   required two-step owner actions, and rollback target;
2. production-asset, merged-wire/visual, missing-H5, stale-H6, overclaim,
   ambiguous-name, auto-authority, and no-rollback packages fail closed;
3. a lifecycle model rejects supersession of proposed/rejected candidates,
   forged actors, missing correction evidence, self-replacement, and stale IDs;
4. promoted-to-superseded history replays deterministically and retains the
   old evidence; and
5. supersession has no import, execution, application, publishing, or runtime
   effect.

Only after that cheaper proof passes should a separately authorized Kernel
change be proposed. No owner approval or promotion phrase should be requested
until the lifecycle gap is closed and the complete Forge gate passes.
