# G1 / C3 optical phase-space transport certificate implementation result

Date: 2026-07-17

Status: **the owner-approved additive transport-certificate sibling is
implemented and focused/platform verified. It remains capability-free evidence
and does not authorize coupling, arrival, power, visibility, runtime,
promotion or C3 closure.**

## Implemented boundary

The new `optical-phase-space-transport-certificate` crate consumes the existing
optical cell, physical path and fixed arithmetic owners unchanged. It replays a
complete 64-bit-admitted immutable origin through at most 64 ordered
same-medium physical face events and derives every correlated face form from
that origin. No existing domain crate source or V1 behavior changed, and no
current coupling or arrival owner imports the sibling.

The package freezes:

- exact Q32.32 direct-face algebra and outward Q160/Q1.62 projections;
- a 490-bit live shield in opaque checked signed-512 storage;
- typed interface, outer, unavailable, ambiguity, non-forward, arithmetic,
  projection and exact work-exhaustion terminals;
- complete nested replay, strict canonical JSON and four SHA-256 identity
  domains;
- 16 MiB input, 256 KiB step, 20 MiB certificate and 40 MiB aggregate byte
  ceilings; and
- deletion-only rollback.

## Integrated falsification and repair

The first Rust integration correctly rejected the original conservative
residual envelope because it was wider than the unchanged physical owner's
certified face receipt. Containment was not weakened and the physical owner was
not modified.

The implementation instead evaluates the exact residual numerator as a
quadratic polynomial over the four shared phase-space symbols and the four
relevant declared remainder intervals. Termwise exact interval bounds retain
the shared endpoint denominator `D*S*V_endpoint*b^2`. The independent repair
oracle passes **20,000** shared-symbol/remainder falsifiers and both tangential
physical-face containment axes. Its source SHA-256 is
`525f8a1a235812da50594de9cf1bac97fa5c86243402dd05f94e843a267d2c21` and
receipt is
`70d171b5c377db29676b4d401a335a6f417124fa739665f62c27956c73daa987`.

## Frozen fixture and resource receipt

The 64-bit `x+, y+, x+` three-face fixture has certificate identity
`3e56fec3951c34f7aa54dc3dc469bf5ba62d289da4785b03ea63e39b4f694f29`.
Its aggregate receipt records:

- observed maximum live width: **481 bits**;
- checked domain arithmetic operations: **8,691**;
- directed divisions: **36**;
- directed projections: **18**; and
- public direct-form scalars: **63**.

The result stays below the frozen 490-bit live shield and all per-package work
ceilings. Valid 64-bit input passes; valid 65- through 70-bit cells fail the
transport admission cap before transport.

## Verification receipts

- native warnings-denied all-target tests: 4 passed;
- executable `i686-pc-windows-msvc` tests: 4 passed plus doc tests;
- `aarch64-linux-android` compilation: passed;
- module boundary and dependency-cycle verification: 48 modules passed;
- module-context generation and freshness: 48 front doors passed;
- original transport oracle: 24 positives and 33 hostiles retained;
- repeated-relinearization width rejection: 513-bit and 778-bit failures
  retained; and
- origin-anchored oracle: 18 equivalence falsifiers and 15 typed hostiles
  retained.

The complete Forge gate passed with exit 0 in **291.8 seconds**. This is the
final promotion-independent regression receipt for the implementation tree.

## Authority and rollback

Every object carries `none_evidence_only`. Interface support, direction
mutation, coupling measure, receiver arrival, magnitude, darkness, visibility,
perception and runtime application remain absent. Rollback deletes only the new
crate, contract, verifier, result and registry/workspace references.
