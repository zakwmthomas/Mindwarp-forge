# G1 GP4 Signal Anchor result

Status: verified and complete after registered full-gate proof
`run-7e5c44dc8f48424a8cec42da756e3127` passed in 590,582 ms.

## Implemented proof

The additive `mindwarp-signal-anchor-vertical` crate constructs one exact
`gp4.signal-anchor.bundle-v1` from caller-supplied fixed C3A authority. The
bundle contains the strict S4 session, C3A input and packet, terminal C4V log,
revision-three and revision-four snapshots, real persistence receipt, exact
four-command chain, authored shadow, exhaustive thirteen-field semantic
digest, exact GP3 situation/approach/threat references, real authored-only GP2
ledger, twenty-five semantic slots and twenty-nine unmeasured requirements.

Received evidence is bounded, strict-decoded, canonicalized and replayed. The
C3A-backed and authored terminal projections and both ledger byte sets are
identical while `world_context` remains explicitly different. Stored log or
snapshot bytes cannot substitute for replay. Threat diversion is present in
GP1 world evidence and absent from GP2 emissions, transitions and lanes.

## Verification to date

- RED integration tests were independently accepted before `src/lib.rs`.
- The focused hostile suite passes 9 of 9 tests.
- Focused formatting and `clippy --no-deps -D warnings` pass.
- Independent source and capability review accepted the bounded candidate.
- The 100.8-second focused integration verifier passed the 9-test GP4 hostile
  suite, retained GP0-GP3 and C4V suites, formatting, focused clippy,
  computation readiness and all readiness hostiles.
- Registered invocation `run-fd385f42fd5a4ee68b5e965463e2d8f1` was
  deliberately aborted after 310,191 ms when pre-closeout audit found missing
  successor-route verifier coverage. It is recorded as failed and superseded,
  never as GP4 completion evidence. Exact closeout successor routing and forged
  route/master/status/dependency/receipt fixtures were added before replacement.
- Replacement invocation `run-7d55ecad743f45bf9be1f115694738d0`
  was immediately aborted after 26,038 ms, before canonical mutation, when
  final preflight found stale GP3 successor wording. It too is recorded failed
  and superseded rather than represented as implementation evidence.
- No filesystem, network, process, runtime, Kernel, Companion or Greenfield
  authority is imported.
- The corrected registered complete gate passed as
  `run-7e5c44dc8f48424a8cec42da756e3127` in 590,582 ms. The separate bounded
  `G1-VERTICAL-CLOSEOUT` evidence receipt is recorded without runtime promotion.

## Nonclaims

This is no runtime, renderer, input adapter, accessibility-conformance result,
performance measurement, broad C4 or G1 closure, C3B result, persistence
expansion or runtime promotion. The recorded bounded closeout remains the
separate `G1-VERTICAL-CLOSEOUT` evidence receipt with `broad_g1=false`,
`runtime_selected=false`, `runtime_containment_pending=true`,
`evidence_only=true` and `promotion_authority=false`.
