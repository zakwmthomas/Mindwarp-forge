# G1 / C3 optical phase-space transport certificate oracle result

Date: 2026-07-17

Status: **the deliberately narrow free-space certificate subject survives the
disposable oracle; a code-facing implementation-readiness audit is the only
permitted next step. No schema or production source is authorized.**

## Result

The independent Python `Fraction` oracle derives every output form from the
complete input cell forms and an ordered transport transcript. Callers cannot
seal claimed output forms into a favourable receipt. The proof binds cell,
scope, reconstruction, band/time, physical recipe/profile, step order and
topology tokens, while returning `authority_effect = none_evidence_only`.

The deterministic receipt is:

- Oracle source SHA-256:
  `a678220e7aebd3ec9e71c4df3a2a791d323848e81b3255da7f66e077aac185b5`
- Receipt SHA-256:
  `17b2edb4757b852470bdd9fab8d813b3b184605b4c555edecb55c18ce8fb197f`
- Positive portfolios: **24**
- Hostile rejections: **33**
- Corner and deterministic interior falsifiers: **18**
- Maximum observed numerator / denominator width: **11 / 11 bits**
- Maximum observed certificate size: **3,105 bytes**
- Ordered certificate identity:
  `6d1007358c0f80aab2f69e2c73b03edf3d83fc19dcda4db152e8ef06dc5aeac9`

The variable-direction plane fixture proves the exact complete time interval
`[30/17, 34/15]`. Its hit axis is exactly constant. The two transverse affine
forms retain conservative exact-rational residuals
`[-1951/2040, 73/72]` and `[-41/60, 133/204]`. All enumerated corners and two
deterministic interior falsifiers remain enclosed; those samples are falsifiers
only and are not the proof. The proof is the complete rational interval
evaluation and residual subtraction.

## What survived

- Identity and zero advance leave all six forms unchanged.
- Positive, negative and correlated fixed advances are exact affine updates.
- Constant-direction plane intersection has zero introduced remainder.
- Variable-direction plane intersection retains the centre gradient and a
  complete conservative rational residual.
- Two safely separated ordered planes preserve explicit topology order.
- Restriction commutes with affine advance, and exact measure remains `1/1`
  through 4, 16 and 64 leaves.
- Shared-symbol subtraction retains `[0/1, 0/1]`; an independent-box rewrite
  widens the same difference to `[-1/2, 1/2]`.
- Identities change with band/time, physical profile, topology and step order.

## Typed stops and hostile evidence

The hostile portfolio rejects wrong or missing provenance identities, extra
bindings, forged outputs, missing/reordered/duplicate steps, changed topology,
remainder understatement, unknown arithmetic, invalid axes and every arrival,
coupling, emission, radiance, power, visibility, measure or authority field.

Nine cases stop before a favourable certificate:

- `unsupported_parallel_or_reversed_plane`: **3**
- `unsupported_nonforward_or_ordering`: **3**
- `unsupported_nonlinear_interface`: **3**

The close second-plane attempt was intentionally not relaxed: conservative
widening made complete-cell forward order unprovable, so it failed closed. All
interface, refraction and TIR steps remain unsupported. No point sample,
rounded square root or existing local interface outcome is promoted into a
whole-cell branch claim.

## Limits and disposition

This result proves only that a capability-free, independently derived,
free-space affine transcript can carry conservative correlated-form evidence.
It does not prove that current code owners expose the exact input records or
can adopt the certificate without a dependency cycle, V1 identity change,
duplicated arithmetic, or excessive widening. It does not classify receiver
coverage and grants no arrival, power, visibility, runtime, promotion or C3
closure authority.

The subject therefore advances to a **code-facing implementation-readiness
audit only**. That audit must inspect exact current public seams, ownership,
identity/codec limits, resource ceilings, platform gates and rollback. It must
stop at a separate serious owner decision before any crate, contract, schema,
dependency, production test or production source is added.

