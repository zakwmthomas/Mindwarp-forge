# G1 / C3 post swept-AABB closure reassessment

Date: 2026-07-16

Status: **implementation fits C3; C3 remains executing; next optical design
question selected.**

## Result against the overall plan

The verified swept-AABB reference fits the `MASTER_PLAN_V2.md` boundary. C3
now owns a bounded observer-independent answer for whether one declared local
extended probe contacts or strictly overlaps validated physical occupancy
during fixed-orientation translation. C6 still owns bodies, locomotion,
physiology and organism-relative meaning. A future runtime adapter may
accelerate the query but cannot redefine it.

This is not permission to form a planet as a sphere. The selected envelope is
a local axis-aligned query box. No world topology, terrain, biome, ecotone,
material, navigation, physics response or presentation system was added.

The result also preserves the biome-continuity rule: categorical cells and
contacts cannot paint visible biome seams. Continuous physical causes must
still produce deterministic fades; sharp boundaries require sharp causes.

## Failure and platform assessment

The permanent shields now cover extended-probe versus point-path confusion,
positive-duration face/edge tangency, initial and outer-domain overlap,
phase-derived mechanics, missing and unavailable evidence, subject-order
identity drift, forged codecs, checked expansion overflow, bounded output and
unchanged substrate rollback. The independent critical-time oracle checksum is
`7d34a367c5cbfb418bf0caca084f1757e9c2539bc110d252c9e7ebf20ab4d43e`.

Warnings-denied x64 tests, executable Windows i686 tests, Android ARM64
compilation, the 65,536-cell debug fixture and the full Forge gate pass. This
supports one portable PC/mobile semantic core. It is compilation evidence, not
actual mobile-device performance proof; a platform fork still requires
measured priority-target evidence. PlayStation remains a later adapter target.

## Why C3 is not closed

The physical-passage substrate is now sufficient for the bounded generic
question it was designed to answer. It does not establish organism
traversability, terrain-scale routes, support, friction, forces or runtime
collision response, and those exclusions must not be relabelled as completed.

The larger remaining C3 physical-visibility gap is multi-interface path
composition. Bulk straight-path transfer and one exact local interface event
exist, but Forge cannot yet construct and bound a piecewise refracted path
through multiple interfaces or prove endpoint transfer. That is a separate
geometry/arithmetic question and must not be coupled to passage.

## Next bounded route

Run a downstream refracted-path composition mathematical design audit. It must
compare bounded piecewise-path representations, exact non-grid event-point
encoding, interface sequencing, volume re-entry, termination, loop and branch
ceilings, accumulated error and replay identity. It must retain total internal
reflection as a terminal local outcome and must not infer perception,
detectability, rendering, real material coefficients or gameplay visibility.

Stop after a design selection and named counterexample oracle. Do not implement
the composer, add coefficients, close or promote C3, or add runtime, organism,
planet, terrain or biome behavior without a later readiness route.
