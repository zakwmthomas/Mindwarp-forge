# G1 C6 body-plan structure V1 implementation result

Status: **bounded implementation verified; stop boundary retained.**

## Implemented result

The standalone `body-plan-structure` crate owns deterministic coordinate-free
family topology, explicit homology and symmetry, and separate lawful structural
expressions. Family identity excludes expressions; expression identity binds
one exact family. Its dependency allowlist is exactly `serde`, `serde_json` and
`sha2`, with no runtime or authority capability.

The synthetic humanoid control is not one locked body. Optional and opaque
conditional structures produce distinct lawful expressions with the same
family identity and different expression identities. That proves the
structural variation seam needed by later dimorphism work while V1 owns no sex
or dimorphism applicability, reproductive role, caste, development or heredity
labels.

`macro-lineage-binding` is the one direct consumer. Its existing candidate
schema, constructor, identity derivation and canonical bytes remain unchanged.
The additive validator accepts only the exact fully validated family identity,
never an expression identity or arbitrary nonzero reference.

## Executable evidence

- 17 body-plan test groups plus one macro-lineage consumer group pass.
- Humanoid family ID: `8b514e7a585efdd41f76479a0869e7907746c2b9f27b6cdcb7ef15df077549f6`.
- Humanoid expression ID: `8fc7a8940a765879fce56d3123cfd42a48b36c28953a253078d40b68dd1b1245`.
- Receipt fingerprint: `fdbdc35205fb0c955c7e436ab53bba60d0bcb61bbf1ca95d4ec7fb3c528ae529`.
- Strict family and expression codecs reject whitespace, trailing content,
  unknown or duplicate fields, schema drift and noncanonical ordering.
- Exact collection maxima pass and maximum-plus-one rejects before graph
  expansion. The canonical byte ceiling is enforced in builders, decoders and
  direct validators; 262,145 raw bytes reject before parsing. This is a safety
  ceiling, not a claim that padding to exactly 262,144 bytes is a valid record.
- Containment cycles and multiple parents reject. Multiple containment roots
  joined by structural connections and structural-connection cycles pass.
- Budget exhaustion is atomic `indeterminate_budget`; nested validations share
  one reported budget.
- The dependency allowlist and whitespace-normalized full source capability
  surface, including aliases and grouped imports, are audited.

## Platform and governance evidence

- Native x64 tests and strict Clippy pass.
- `i686-pc-windows-msvc` tests execute on the same host and pass.
- `aarch64-linux-android` compiles successfully; this is compile-only, not
  execution proof.
- Modularity, module-context freshness and record-role checks pass with one
  direct consumer.
- Independent schema, matrix and integration reviews were applied before this
  result was recorded.
- Registered full Forge gate `run-e923d74fb96743e5864b303faf27acfc`
  passed the complete repository scope in 874,785 ms. The earlier
  `run-57db0fb7534941d7a536bea252149e6b` failed on a stale no-op hostile
  mutation and is retained as diagnostic evidence, not completion evidence.

## Stop boundary

This result does not activate ecology, physiology, reproduction, heredity,
development, dimorphism applicability, caste, species, individual/population
semantics, personhood, geometry, pose, assets, animation, rendering, runtime,
C7, Companion, Greenfield, Kernel mutation, approval or promotion. The next C6
package requires a separate owner route.
