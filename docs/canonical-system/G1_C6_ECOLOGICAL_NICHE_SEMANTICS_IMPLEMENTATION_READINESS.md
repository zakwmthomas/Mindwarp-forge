# G1 C6 ecological-niche semantics implementation readiness

Date: 2026-07-20

Status: **code-free authored-hypothesis candidate frozen for one future
test-first implementation decision; no source authorized.**

## Frozen candidate

The candidate is exactly the strict V1 model in:

1. `G1_C6_ECOLOGICAL_NICHE_SEMANTICS_MATHEMATICAL_DESIGN_AUDIT.md`;
2. `contracts/ecological-niche-semantics-contract.md`; and
3. this readiness record.

A later implementation may add one capability-free
`ecological-niche-semantics` crate and one additive public binding in
`organism-niche-binding` only after a separate exact owner authorization. This
readiness stage adds neither.

## Dependency and ownership freeze

The future owner consumes exact received records from `derived-world-rules`,
`niche-graph-binding` and `organism-subject-identity`, plus only semantic-neutral
codec/hash libraries already admitted by the workspace. It must not require
macro-lineage or body-plan as direct dependencies when validated identity is
sufficient.

The future consumer alone may depend on the new owner. C3, body-plan,
macro-lineage and identity remain upstream and byte-identical. There is no
runtime, persistence, filesystem, network, process, clock, randomness,
database, UI, renderer, Forge Kernel or approval capability.

## Record, codec and identity freeze

Implementation must reproduce every record, field, closed enum, domain,
canonical ordering, exact replay rule and nonclaim in the contract candidate.
Unknown, duplicate, missing, reordered, noncanonical or trailing input fails
before output. Equal numeric values never repair changed provenance. Trophic
direction and competition symmetry remain distinct. Species-candidate identity
never becomes membership.

The pre-source contract/design/readiness file hashes are retained by the
future implementation verifier. Any semantic change returns to design; it may
not be hidden in source.

## Resource and atomicity freeze

The future source must enforce the exact `16,384` hypothesis bytes, `32,768`
receipt bytes, `12` predicates, `2` subjects, `1` referent, `12` graph nodes,
`66` edges, `128` examinations and `1` output record ceilings. The exact
counted requirement is `16 + |V| + |E| + |predicates| + 4*|subjects|`.

Permanent fixtures cover every exact maximum and maximum-plus-one boundary.
Required budget passes; required-minus-one returns `indeterminate_budget`
before semantic work. Cumulative preflight occurs before allocation or replay.
Every invalid input, stale identity, capability leak, panic, overflow or budget
failure produces no partial record.

## Frozen positive matrix

The later implementation must prove all fourteen design fixtures, including:
supported, unavailable, contradictory and budget-indeterminate receipts;
inclusive interval boundaries; intersection contradictions; exact provenance
identity sensitivity; predicate permutation stability; two subject kinds;
humanoid and structurally distinct radial controls with no sex/dimorphism
meaning; trophic direction including a cannibalism candidate but no detrital
pathway; equal- and unequal-endpoint competition canonical ordering with
plurality, demand, limitation and interaction unresolved; and one additive
consumer binding that leaves the existing environmental-support result
unchanged.

No fixture may claim positive biological suitability. The strongest positive
is `necessary_evidence_supported` with `ecological_truth_status: unresolved`.

## Frozen hostile matrix

The future source and present document-fixture verifier retain exact
`C6-H100..C6-H105` and applicable `C6-H1100`, `C6-H1101`, `C6-H1102`,
`C6-H1103`, `C6-H1105`, `C6-H1106`, `C6-H1108` meanings from the mathematical
design. `C6-H1104` and `C6-H1107` remain later closure work.

Additional permanent hostiles reject:

- stale, cross-world, cross-subject, cross-kind or substituted evidence;
- physical opportunity as a complete niche;
- moisture/exposure/partition as habitat or suitability;
- liquid as resource access, yield or renewal;
- pressure, atmosphere or signal as harm, viability or sense;
- coavailability as trophic flow or competition;
- equal competition endpoints as proof of self-competition or multiple
  competitors;
- prospective association as realized occupancy or evolution;
- physical boundary/disposable oracle as production ecotone;
- species/population identity as membership;
- body-plan expression as sex, caste or dimorphism;
- missing as zero/false, contradiction as impossibility, or budget exhaustion
  as a negative ecological result;
- scalar ranking, probability, interpolation or cross-packet/time/scale
  generalization;
- duplicate/reordered/unknown/trailing codec input;
- predicate/range/type poison and maximum-plus-one resources;
- partial output, a second consumer, dependency cycle, predecessor mutation;
  and
- capability or authority vocabulary/reachability.

## Platform and integration gates

An authorized implementation must pass:

1. warnings-denied native x64 execution;
2. same-host i686 execution;
3. Android ARM64 compile-only, never reported as execution;
4. strict codec parity and stable identity vectors;
5. formatting and strict Clippy;
6. retained C3, body-plan, macro-lineage, identity and organism-niche tests;
7. capability and dependency scans;
8. the exact one-consumer integration and no-cycle shield;
9. independent biology, schema/security and integration/governance review;
   and
10. one registered measured complete Forge gate before any result is recorded.

Failure of any semantic, resource, portability, integration or complete gate
returns to design or deletes only the new crate, additive consumer binding and
their route references. Predecessor bytes and identities remain unchanged; no
data migration exists.

## Implementation non-authority and exact future decision

This readiness record does not create a crate, Cargo member/dependency,
production source, consumer implementation, implementation result, runtime
adapter or canonical ecological truth. It does not activate physiology,
reproduction, heredity, development, realized occupancy/evolution,
species/ecomorphs, dimorphism, population semantics, comparison or C7.

The next owner decision, if released, is one test-first capability-free V1
implementation of the frozen candidate and its single additive consumer. Any
aggregate niche portfolio, realized truth reducer, second consumer, product
vocabulary or broader package is separately gated.

Stop here after readiness verification. Nothing broader is locked in. One
consumer first, reassess before expanding.
