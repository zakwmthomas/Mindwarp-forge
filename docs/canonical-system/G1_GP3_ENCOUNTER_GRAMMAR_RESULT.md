# G1 GP3 encounter grammar result

Status: promoted and recorded after independently accepted implementation and
complete registered Forge gate `run-50a8c78043eb46c483f1f655d3793f9b`.

## Result

GP3 now provides exactly five fixed authored encounter situations over the
five canonical GP0 sessions. The registry contains the exact multi-domain
facets, evidence and risk references, 18 distinct approach IDs, fixed prepared
tools, typed ordered steps, typed prerequisites, per-risk dispositions, causal
explanations and limitations frozen by the accepted design package.

Every approach references one exact GP0 outcome. Its abstract consequence set
covers every mutation, fixture-owned opportunity cost, memory, grant and named
decision by kind, ordinal and framed canonical digest exactly once. GP3 returns
borrowed GP0 consequences; it does not duplicate their payload or map them into
GP2 progression.

S3 force is partial only. Every situation retains at least two threat-free
noncombat causal routes plus retreat. Optional threat composition is exact,
approach-bound and nonterminal only in S2, S4 and S5. S5 requires the exact
latest replay-valid S1 direct, bypass or ration event and rejects missing,
stale, reordered, foreign, fabricated or retreat history.

## Strict authority boundary

Five situation digests and one grammar digest are pinned with unsigned
big-endian domain framing. Grammar and direct-situation codecs reject unknown,
duplicate, trailing, reordered, oversized, malformed and noncanonical inputs.
Nested IDs, text and vectors are bounded before fixed-registry semantic
equality. Public resolution and threat helpers authenticate the exact canonical
session, situation and approach before returning borrowed values.

The implementation adds no procedural generation, weights, probability,
random selection, runtime execution, persistence, filesystem, network,
Greenfield, GP2 mapping, C3B authority, GP4 implementation or Kernel mutation.

## Verification

- independent pre-source design acceptance passed after correcting nine
  authority and completeness gaps;
- independent candidate acceptance passed after final helper/session and
  direct-situation codec review;
- GP3 focused hostile matrix: 18/18 passed;
- retained GP2: 7/7 passed;
- retained GP1: 7/7 passed;
- retained GP0: 14/14 passed;
- `cargo check --workspace` passed;
- module context: 54 modules passed;
- durable record roles: 906 files passed;
- worker checkpoint and bootstrap verification passed.

The complete registered Forge gate passed in 490027 ms as
`run-50a8c78043eb46c483f1f655d3793f9b`. GP3 is promoted, recorded and closed.
GP4 remains dependency-ready but owner-gated; stop before GP4 and runtime.
