# H1 Reference Intake Result

Status: bounded repository-local reference intake verified. No network
acquisition, bulk extraction, executable content, engine work, promotion, or
owner-observation inference occurred.

## Inventory

The authoritative recovered archive is
`forge documents from gpt handover/MINDWARP_FORGE_CONTINUATION_SURVIVAL_PACK_2026-07-12.zip`,
SHA-256 `f0f01b7469226d3d5c77780c23e97a96342b517ece536bc0351e9486117b251b`.
Direct archive inspection found no recovered mesh, rig, or visual target: no
OBJ, FBX, glTF/GLB, Blend, DAE, STL, PLY, PNG, JPEG, WebP, or SVG humanoid/model asset. Three humanoid-relevant JSON
reports exist:

| Entry | Bytes | SHA-256 | Classification |
|---|---:|---|---|
| `one_button_humanoid_blueprint.json` | 5,430 | `74b23331be5291bf399cd4d4b364059de7ab4d305569e19e7090470f73502491` | Declared legacy structural blueprint; minimal-suite challenge evidence only |
| `one_button_humanoid_vertical_slice_report.json` | 95,288 | `3027c877ff79007a90385b0ad3fdde0dcc59c42c91e0fdef9c90a005bcd21dff` | Self-reported run log; retained adversarial evidence, not a reproduced result |
| `asset_category_contracts.json` | 6,205 | `1fe453c604ce855c2046707ae55fc71c782d2f5bf8b0dd70847aab4b6555eac0` | Category-first legacy policy; retained contrast evidence, not Forge architecture |

The Android emergency pack contains byte-identical copies of all three entries.
It adds no independent humanoid evidence. The second upload remains recap-only.
Disposable Forge chat screenshots are presentation artifacts bound to earlier
synthetic controls; they are neither recovered reference targets nor durable
canonical evidence.

## Poison and overclaim audit

The blueprint declares a bilateral biped hierarchy and useful named interfaces,
but it also embeds the old category-first approach, an “80 percent” goal, a
`0.803` aggregate quality score, and an engine-neutral handoff-ready claim. The
vertical-slice report claims a 2.26 ms uncached run, 1.27 ms cached run, no
manual intervention, no failures, deterministic cache reuse, and automatic
success. None was independently rerun against retained source and artifacts.

Therefore only the declared hierarchy/interface shape is admitted as a claim
candidate. Scores, timings, success flags, category authority, quality levels,
automatic repair, mesh/voxel generation, rig binding, deformation, and runtime
readiness are explicitly excluded.

## Minimal H1 suite

`h1-neutral-humanoid-reference-intake-v1` contains exactly two independent
targets: the recovered blueprint challenge and Forge's typed synthetic v3
T-pose fixture. The former is evidence-only; the latter is Forge-owned and
deterministically structural. Both forbid canonical-baseline use, production
import, runtime execution, perceptual approval, and numeric acceptance
thresholds.

Six `reference-intake` tests verify deterministic bytes/fingerprint,
provenance diversity, duplicate-hash rejection, recovered-claim authority,
mandatory forbidden uses, executable/path/network/markup rejection, unknown
field rejection, and version drift. The module has no filesystem, process,
network, desktop, or protected-Kernel capability.

## Limits and next route

H1 proves safe intake and classification, not reference adequacy. There is no
licensed external visual humanoid target in the repository, so no visual
convergence or generalisation claim is available. H2 should now define the
stable neutral humanoid representation contract and generate Forge's own
structural artifact from declared roles and constraints. External references
remain unnecessary until a later, separately authorized acquisition package.
