# G1 / C3 disposable cross-boundary ecotone oracle result

Date: 2026-07-18

Status: **the independent exact-rational fixture oracle survives the frozen
disposable falsification package. This is proof-tool evidence only; it grants
no production owner, schema, dependency, consumer, renderer or C3 closure.**

## Result

The owner-approved standard-library Python oracle passed twice with
byte-identical canonical output under isolated `-I -B` execution. The two
observed runs completed in 63.0 and 62.7 seconds, below the frozen 120 seconds
per-run budget. Canonical stdout is one 8,909-byte JSON line including the
Windows newline, below 64 KiB, and the proof's traced peak remained below the
256 MiB ceiling.

The suite visited 469,833 cells and 934,472 shared edges across seven real
enumeration manifests. The maximum fixture contains 65,536 cells and 130,560
internal edges; `257 x 256` fails in preflight before evaluation. Exact integer
half-up arithmetic agrees independently with `Fraction`, with the maximum
observed rounded product requiring 39 bits.

All 19 hostile families pass as executable mutations rather than labels. They
cover label-only and categorical-boundary nuisance changes, deterministic
cell/edge enumeration, disconnected islands, unavailable-versus-zero evidence,
explicit and missing sharp witnesses, equal-valued sharp causes, steep
continuous exposure, exact ramps and reversal, rounding thresholds, type/range
and coordinate poison, provenance substitutions, stale/duplicate/missing
partition evidence, dimension-local contradiction, diagonal/wrap/unknown joins,
aligned refinement plus moved-coordinate rejection, recursive blending,
fixed-width categorical halos and heterogeneous-evidence collapse.

## Pinned identities

- Oracle: `tools/prove-g1-c3-cross-boundary-ecotone.py`
- Oracle SHA-256:
  `60253d2fea9a06109df5ac81ae1f0f80846f872c5a4271af7fbb088f7cff201d`
- Canonical stdout SHA-256:
  `3b281dcaf2e6e8a9053c7cbeefd0dd8b971c07dc1f5da45fd51888fd20c39c2a`
- Internal suite receipt hash:
  `8bcb3bd741ba6913a85a4ca3c26170943d6e54609bb44aa867c0cebff1f44b24`
- Enumeration modes: `7`
- Hostile families: `19`
- Positive fixtures: `13`
- Preflight resource rejections: `1`
- Evaluated cell visits: `469,833`
- Evaluated edge visits: `934,472`

The semantic digest commits exact coordinate-keyed causal cell results and
typed edge results while excluding fixture names, annotations and execution
order. The audit digest separately commits fixture input and execution mode.
Thus relabeling and a moved categorical boundary preserve semantics while
changing audit identity; a moved physical coordinate changes semantics.

## Independent boundaries

The tool imports only Python standard-library exact arithmetic, hashing, JSON,
bounded threading and allocation tracing. It reads no Forge source or output,
uses no production helper, filesystem input, network, subprocess, FFI, external
package, random source, clock-derived semantics, float arithmetic or GPU.
GPU execution is inappropriate here because the workload is small exact-integer
arithmetic plus branch-heavy provenance and topology validation.

The synthetic material-interface witness is fixture-local. It is not a
canonical 2D material-interface owner and cannot establish physical scale,
received energy, visible seamlessness, perceptual quality, ecology, organism,
renderer or runtime behavior.

## Verification

Focused proof execution passes twice with byte-identical output.

- Complete command: `powershell -NoProfile -ExecutionPolicy Bypass -File tools/verify.ps1`
- Exit code: `0`
- Wall time: `444.7 seconds`
- Captured output lines: `2,508`
- Durable files classified: `853`
- Declared modules verified: `52`

## Surviving claim and boundary

Only this claim survives: for the frozen synthetic fixture model, the existing
pointwise palette relation can be replayed exactly and independently while
remaining invariant to category labels and deterministic traversal order,
retaining explicit sharp material causes, and preserving unavailable,
unsupported, contradictory and provenance-mismatch outcomes without inventing
a blend.

No production owner, contract, crate, dependency, production source, production
test, downstream consumer, renderer, biome, organism, runtime, promotion or C3
closure is authorized. Rollback deletes only this script, focused verifier,
result record and their route references.

Nothing broader is locked in. One consumer first, reassess before expanding.
