# G1 / C3 calibrated spectral/time basis oracle result

Date: 2026-07-17

Status: **the additive calibration witness survives exactly; physical
received-energy composition remains blocked on whole-cell transport
applicability and spatial calibration. No schema or source is authorized.**

## Result

The exact-rational oracle passed twice with byte-identical output. A separate
versioned witness can bind three ordered, contiguous half-open vacuum-
wavelength intervals and one exact half-open SI time cell to unchanged legacy
RGB/time pairs. The three bands add to one declared covered-domain radiant
energy, and adjacent time cells add only under identical origin and tick
scale.

The witness identity changes under any spectral interval, weighting, time
origin, tick interval, seconds-per-tick, provenance or version change. The
oracle rejects multiple physical meanings for one legacy pair, so V1 identity
equality never silently becomes physical calibration equality.

## Transport theorem and counterexample

Five nonnegative spectral/time allocation portfolios produced the same
conservative integrated-energy bounds when the transport receipt supplied a
whole-cell pointwise enclosure. The proof uses only monotonicity: if every
point transfer lies in `[l,u]`, the integral lies in `[l*Q,u*Q]`.

The oracle also constructed equal-total-energy sources concentrated in
opposite spectral halves. Under transfers `[1,0]`, received energies were `2`
and `0` joules while an asserted average predicted `1`. The temporal analogue
produced the same failure. A midpoint, band average or representative sample
therefore cannot authorize physical composition from integrated energy alone.

## Pinned receipt

- Oracle: `tools/prove-g1-c3-calibrated-spectral-time-basis.py`
- Oracle SHA-256:
  `b8e94899d4b49f93416d4ffd054b7b028e3bdc5fd0c4b235d39db41592c6b8d5`
- Receipt checksum:
  `0244df63a2ca1e2a58b3c035104c1d2997fcfe4a693fed872ae1262eb3b64c78`
- Additive spectral bands: `3`
- Allocation portfolios: `5`
- Hostile rejections: `34`
- Weighting: `unit_energy_integral`
- Legacy V1: `unchanged_identity_only`
- Complete Forge gate: passed in `440.8` seconds across `2,209` output lines;
  `797` durable files were classified and all `50` module front doors remained
  current.

The numeric wavelength and time values used by the oracle are disposable
fixtures. They are not selected RGB boundaries, a tick duration or a clock
origin.

## Remaining physical block

The current visible-radiance transport owner does not prove pointwise
spectral/time validity for its scalar band result and explicitly does not
calibrate coefficients per abstract volume coordinate unit to SI space. The
basis witness therefore survives as source-subject calibration but cannot
make the existing dimensionless result a physical received-energy claim.

Physical composition requires a separate applicability receipt binding the
exact calibration witness, transport profile/revision, spatial calibration
and whole-cell pointwise enclosure. Absence, ambiguity, sample-only evidence
and foreign calibration all fail closed.

## Decision

Advance one bounded step to a **code-facing calibrated-basis and transport-
applicability schema gap audit**. It must inventory exact owners, codecs,
identity uniqueness, resource ceilings and rollback, and decide whether a
source calibration basis can advance independently while transport remains
blocked.

Do not implement. Add no crate, dependency, contract schema, production test,
production source or registry. Select no normative wavelength, tick, epoch,
source spectrum, spatial scale or coefficient catalogue. Do not claim
received energy, power, irradiance, radiance, detector response, visibility,
runtime, promotion or C3 closure.

Nothing broader is locked in. One consumer first, reassess before expanding.
