# G1 GP0 gameplay foundation result

Status: bounded prototype tested; no runtime or production promotion.

The owner-authorized GP0 package implements the
`mindwarp-gameplay-foundation` crate as a capability-free proof of the
`causal_explorer_maker` player fantasy. It freezes one typed concept and five
corrected authored sessions without importing historical runtime code or
granting GP1, engine, network, monetization, publishing, or Greenfield
authority.

## Implemented proof

- strict canonical codecs and validators cover concept, session, path-free C3A
  reference, reducer state, and append-only history;
- every branch, including deterministic retreat, has exact typed mutation,
  cost, memory, non-spendable consequence, and named-next-decision data;
- combat contributes in S2, S4, and S5 but reaches zero terminal states by
  itself and is rejected in sessions without a named threat contribution;
- S4 clearing wire scavengers changes only the work area while the failed
  anchor and stranded Iven remain unresolved;
- S5 replay requires one exact admitted S1 predecessor. Direct, bypass, and
  ration produce different colony mutations and Keeper Mara memories; retreat
  is rejected and S1 remains the first ordered history event;
- fabricated public state, nested history corruption, currency leakage,
  authority drift, inaccessible risk cues, action-order drift, foreign C3A
  packets, and noncanonical bytes fail closed;
- text output is deterministically generated from typed state.

The focused Rust suite contains 14 integration tests at the initial result
receipt. `tools/verify-g1-gp0-gameplay-foundation.ps1` is the retained focused
gate; later verification receipts may increase that count without changing the
contract.

## Authority and evidence limits

All fixed-session physical and scientific-looking facts are explicitly
`AuthoredGameplayNonC3B`. The executable C3A test proves only that an exact
`WorldGenerationInput` / `CausalWorldPacket` pair can supply a typed identity
reference and that a foreign packet is rejected. It does not validate the
authored storm, crystal, habitat, signal, timing, ecology, or presentation
facts, and it does not close C3B.

The default session duration, stable stops, fixed hub or vessel, and repairable
failure remain reversible proposed assumptions. GP0 does not authorize GP1 or
runtime implementation.

## Verification ledger

- The first focused verifier attempt failed because a static contract token
  crossed a Markdown line break. The token matcher was corrected; no Rust or
  gameplay invariant failed.
- The focused gate then passed all 14 integration tests.
- Complete run `run-da155642b3694a85928325cebdfd6888` failed after 42,548 ms
  because the shared C3 route policy still listed only GP0's former adjudication
  substage. The repair adds only the exact GP0 verification substage and its
  restricted authority pattern; it does not admit another package or broaden
  gameplay, C3B, or runtime authority.
- Complete run `run-79fca2c134994b76a119aebc0987a4fd` then passed after
  408,744 ms against checkpoint
  `524a1d8ffaaaad2cc4981a2800c911fd025c6fc7195dbf488244053d96d36788`.
