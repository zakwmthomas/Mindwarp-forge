# G1 / C3 whole-cell dimensionless-transfer oracle result

Date: 2026-07-17

Status: **the conservative same-medium optical-depth composition survives the
exact-rational hostile oracle; implementation remains blocked on explicit
band/time binding and nonduplicated exponential-kernel ownership.**

## Deterministic receipt

The oracle source SHA-256 is
`486495cdee971e000cf8f326aa2b895247ce9f4b8ed35f5e5832723e516450c1`.

Two runs produced the identical canonical checksum
`b6cac30017fa55bee50aff399d61027b91c8696d96d4236508aaef612ca4dc91`.

The retained receipt contains:

- 16 finite, vacuum, opaque, zero, unresolved, unavailable, ambiguous and
  interface portfolios;
- nine exact sample-containment checks;
- ten hostile subject/authority rejections;
- exact 4-, 16- and 64-child measure conservation;
- a central-lane substitution counterexample;
- repeated-Q0.48 avoidable-underflow evidence; and
- the derived finite 64-step Q64.64 prefix ceiling below 118 raw bits.

## Surviving receiver rules

For receiver-face entry at selected step `k`, mandatory complete steps before
`k` contribute their full lower and upper optical-depth bounds. The selected
partial step contributes zero lower depth and its full-step upper depth. Every
tested exact member remains inside the resulting monotonic attenuation
enclosure.

Step-zero start-inside remains identity. Later start-inside remains wider
because the existing result does not identify the earlier first-entry step.
Mandatory prior opacity proves exact zero; opacity confined to the selected
partial step or an uncertain pre-entry history widens conservatively instead
of fabricating zero.

Zero coupling carries no receiver transfer. Unresolved, unavailable,
ambiguous and interface evidence retains unresolved measure and emits no
factor.

## Counterexamples retained

One central optical depth can coexist with a complete-cell range from 1 to 7;
the central transfer does not enclose the boundary transfer and cannot be
substituted.

Multiplying two positive raw Q0.48 values equal to one produces zero under
floor projection while the exact product is `2^-96`. A zero lower bound caused
by projection is therefore numerical underflow, never opaque evidence or
darkness. The selected design evaluates accumulated optical depth once in the
oracle and preserves this distinction.

## Code-facing blockers

The transport certificate's `band_time_id` is intentionally opaque while the
bulk owner accepts an RGB enum. An additive deterministic band/time binding is
required before cross-band replay can fail closed.

The verified bulk exponential kernel is private. Readiness must compare a
capability-free shared kernel, a bulk-owned read-only evaluation receipt and a
conservative public-factor fallback. Duplicating the kernel or silently
modifying bulk V1 is rejected.

## Disposition

The mathematics survives, but no schema or crate is authorized. The next safe
action is a code-facing readiness/gap audit for band/time identity, kernel
ownership, exact codecs, resource ceilings, platform gates and deletion-only
rollback.

Do not add production source, dependencies or runtime integration. Do not
claim source magnitude, radiance, received power, detector response,
visibility, perception, darkness, rendering, gameplay line of sight,
promotion or C3 closure.

## Verification receipt

The complete Forge integration gate passed with exit 0 in **299.6 seconds**
across 2,011 output lines. It retained every historical C3 mathematical,
provenance and authority shield, the permanent oracle verifier, 771 durable
record-role classifications, 49 module front doors, platform builds and
workspace/UI regressions.
