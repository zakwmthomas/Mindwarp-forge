[package]
name = "physical-path-substrate"
version = "0.1.0"
edition.workspace = true
license.workspace = true

[dependencies]
fixed-interval-arithmetic = { path = "../fixed-interval-arithmetic" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
sha2 = "0.10"
