[package]
name = "viewport-stimulus"
version = "0.1.0"
edition.workspace = true
license.workspace = true

[dependencies]
perception-protocol = { path = "../perception-protocol" }
reference-viewport = { path = "../reference-viewport" }
serde = { version = "1", features = ["derive"] }
sha2 = "0.10"
