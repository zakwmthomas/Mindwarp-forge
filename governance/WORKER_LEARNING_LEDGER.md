# Worker Learning Ledger

| ID | Observation | Change | Expected benefit | Regression signal | Status |
|---|---|---|---|---|---|
| WL-001 | Heartbeats produced micro-task narration | Package-completion batches and Batch State | Higher work per turn | Partial edit reported as completion | Active |
| WL-002 | Prompt improvements could be lost in app settings | Canonical Worker Prompt Specification | Durable reusable behavior | Automation diverges from specification | Active |
| WL-003 | Readiness was mistaken for closed gaps | Macro-to-micro closure queue and P6 | Prevent premature milestones | Gap lacks evidence/test but milestone advances | Active |
| WL-004 | Governance-focused automation prompt could omit explicit package execution | Require automation to state both governance loop and package-completion duty | Self-governance improves work rather than replacing it | Worker reads governance but does not advance Batch State | Active |
| WL-005 | A heartbeat repeated a completed result instead of advancing work | Treat acknowledgement-only wake-ups as non-batches; continue into the next eligible package or remain silent | No context spent on false progress reports | A heartbeat reports no new durable result, test, or recorded blocker | Active |
| WL-006 | Shared metrics could be mistaken for one global productivity score | Federated measurement: shared protocol plus local scorecards and target-local transfer trials | Reuse good methods without averaging away local regressions | Aggregate recommendation lacks per-module breakdown or hides negative transfer | Active |
| WL-007 | W1 full-gate closure entered the valid `verifying` state but its own fixture only accepted `executing` or `checkpoint` | Exercise every non-terminal resume state in worker batch fixtures | Prevent the proof harness from rejecting its real closure lifecycle | Full gate fails solely because an allowed in-progress state is excluded by the fixture | Active |
| WL-008 | The batch fixture copied live state and imposed an unnecessary non-terminal precondition, so the post-closure gate failed on valid `complete` state | Make lifecycle fixtures self-contained and independent of the live batch state | Allow the same full gate to prove active, verifying, and completed workers without hidden phase coupling | A fixture result changes solely because the live worker advances between valid states | Active |
| WL-009 | W2 atomic outbox writes exposed PowerShell empty-array and single-item shape traps during offline retry | Serialize arrays explicitly, normalize nested array shapes, and reconstruct delivered envelopes instead of mutating deserialized objects | Prevent queued notifications from disappearing or failing when the queue contains zero or one item | Offline or acknowledged-retry fixtures fail on empty/single-item outboxes | Active |
| WL-010 | The A1 replay fixture attempted Windows directory cleanup while the reopened SQLite connection still held the database | Explicitly drop reopened persistence handles before fixture cleanup | Keep replay tests deterministic on Windows and prevent false implementation failures from locked files | Any database-backed fixture removes its directory before all connections are dropped | Active |
| WL-011 | A2 low-level envelope retries could repair missing history, but the high-level compiler import returned around that repair path when the envelope already existed | Route equal high-level retries back through idempotent envelope persistence | Recover a crash window between envelope and history writes without duplicate projections | Deleting one history projection and retrying the exact import fails to restore exactly one record | Active |
| WL-012 | A2 closure updated the hashed closure register after the prior feedback refresh, so the post-transition gate rejected stale worker feedback | Refresh generated worker feedback after final durable closure records and before the post-transition gate | Ensure the next worker receives fixity-checked final routing rather than a pre-close snapshot | Any post-transition gate reports a stale hash for a record changed during closure | Active |
| WL-013 | A3 hostile-link setup first failed with Windows error 1314, and its traversal fixture tried to promote paths already rejected by admission | Use a local junction for the live ancestor boundary and assert traversal rejection at the earliest enforcing layer | Keep hostile-path proof executable under restricted Windows accounts and aligned with fail-closed layering | A path fixture requires symlink privilege or unwraps input that an earlier validator correctly rejects | Active |
| WL-014 | Two parallel B1 SQLite fixtures initially shared a PID-derived directory namespace and one could delete the other's live database, surfacing as Windows error 32 | Give every fixture a package-and-purpose-specific temporary namespace | Preserve parallel test isolation without retries, sleeps, or false lock diagnoses | Two concurrently runnable tests resolve to the same temporary database directory | Active |
| WL-015 | The first B2 reopen fixture projected gate receipts alphabetically by ID instead of lifecycle insertion order | Give control records an append-ordered projection while retaining immutable IDs | Preserve exact stage history and make stale-transition validation replayable | Reopened gate order differs from the order in which lifecycle receipts were accepted | Active |
| WL-016 | The heartbeat scheduler woke again at an unchanged terminal owner/design gate after autonomous retry was prohibited | Emit one deduplicated efficiency escalation and pause/delete the scheduler until owner input materially changes the route; if scheduler control is unavailable, record that limitation once | Prevent repeated context reloads, duplicate blocker narration, and invalid no-progress wakes | A heartbeat fires again with the same blocked gate revision and no new owner input | Active |
| WL-017 | The active work package and Atlas advanced to F5, but the generated first-layer task briefing still routed new workers to the completed F4 owner gate | Refresh the task briefing inside bootstrap verification and compare its work-package and milestone route to the active record | Prevent a fresh worker from following a stale first-layer handoff before it reaches the newer active state | Bootstrap passes while `BRIEFING.md` names a different package or milestone than `WORK_PACKAGE.json` | Active |
| WL-018 | After the F5 transition, the verified Atlas and refreshed task briefing were current but the first-layer roadmap still called F4 active and F5 planned | Make Atlas verification compare the roadmap's declared active milestone to `project-model.json` | Keep every first-layer route consistent instead of protecting only generated briefing state | Atlas verification passes while `ROADMAP.md` declares a different active milestone | Active |
| WL-019 | A nested `powershell -File` call converted the owner-notification Boolean argument to text, while direct invocation was blocked by the host execution policy | Invoke Boolean-bearing scripts through a bypassed `-Command` script block that preserves `$true`/`$false` as Boolean values | Prevent a valid notification from being mistaken for a product failure or silently skipped | A Boolean script parameter receives `System.String`, or direct invocation fails before notification routing | Active |
| WL-020 | The P4 gate asked the owner to reason about hierarchy/save architecture before directly reconciling the fixed recovered master specification, which already settled the product-level model | Before each new module or owner question, run targeted repository and recovered-evidence retrieval; separate accepted project truth from genuinely new engineering defaults, then ask one plain yes/no question | Reduce owner cognitive load and prevent rediscovery or accidental redesign of accepted architecture | An owner is asked to choose a module architecture without a cited targeted check of existing project evidence | Active |
| WL-021 | Active package facts were manually repeated across work-package, batch, current-state, handoff, and briefing files; they drifted and forced workers to rewrite prose instead of using F2/F4 continuity | Make `WORKER_BATCH_STATE.json` the sole active checkpoint, generate compact views, delete redundant authored active files, and test drift/repair | One state transition updates every startup view without contradictory status-file spaghetti | A worker hand-edits a generated view, creates a parallel active-state file, or bootstrap exposes a route different from the canonical checkpoint | Active |
| WL-022 | An unchanged F5 owner gate produced 120 consecutive `no_work` outcomes after its blocker while the owner was asleep | After five unchanged owner-wait wakes, route only to a dependency-ready package independent of the waiting gate; otherwise deduplicate and retire/pause the heartbeat when possible. The owner explicitly exempted the current F5 wait | Reduce repeated context, token, tool, and owner-attention cost without bypassing decisions or damaging dependencies | A sixth wake reloads the same unchanged owner gate, or a fallback selects the gate, one of its descendants, or an item with unsatisfied dependencies | Active |
| WL-023 | Even deduplicated owner waits still fired the scheduler, interrupted chat, and consumed bootstrap/context tokens; visual gates also required the owner to rediscover how to request screenshots | Add atomic heartbeat pause/resume control, pause on the first recognized owner gate, resume only on material user-authored input, and make exact current-fixture screenshots plus a plain chat answer format mandatory for visual gates | Zero scheduled wakes while owner input is pending and no repeated request needed for remote visual review | A heartbeat fires after the automation enters `PAUSED`, an unrelated message resumes it, or a visual owner gate is presented without exact screenshots and a response format | Active |
| WL-024 | Parallel worker rollouts shared the parent conversation session identifier, so the visible-conversation scanner treated internal worker files as replacement owner sources and paused capture | Exclude rollouts carrying subagent source metadata or a parent thread identifier before source selection, and retain a parent-plus-subagent collision fixture | Parallel work cannot poison, duplicate, or pause owner-conversation capture | Spawning an internal worker changes the captured owner source, creates internal evidence, or pauses the parent cursor | Active |
| WL-025 | A locally generated screenshot could capture an occluded or wrong window and a file path was previously mistaken for successful chat delivery | Require exact PID and title identity, non-occluded HWND rendering, visible on-screen bounds, distinct source hashes, provenance sidecars, and an explicit `chat_delivery_performed: false` receipt | Visual handoffs are reproducible, window-scoped, tamper-evident, and never confused with actual delivery | Capture succeeds for an ambiguous, minimized, off-screen, blank, tampered, identical, or wrong-window input, or local generation claims the chat received it | Active |
| WL-026 | A technically readable visual asset can still be a poor or inaccurate comparison target, especially for humans | Require pixel-level fitness, provenance, anatomical and comparison suitability, explicit limitations, and owner review when uncertain | Bad references cannot silently define quality or make later comparisons meaningless | An asset is admitted from metadata or hash alone, or a human comparison uses a visibly unsuitable reference without review | Active |
| WL-027 | Macro intent and micro implementation facts can diverge when a worker changes stages without refreshing both | Bind a two-scale context refresh to the exact active substage and fail stale stage IDs | Every implementation starts from current project direction and exact local invariants | Batch substage changes while its recorded context still names the prior stage | Active |
| WL-028 | Expensive integrated PC runs can be attempted before cheap logical or simulated failures are removed | Require a cheapest-sufficient execution ladder and evidence-backed escalation | Better initial designs with lower token, time, compute, and risk cost | A costly tier runs without lower-tier evidence, information-gain rationale, cost, and stop condition | Active |
| WL-029 | A fail-closed visual gate also needs a safe state for source discovery and pixel inspection before any candidate can pass | Permit `required_pending` only while dependent implementation is explicitly blocked; retain only rejected or owner-check receipts until a verified-fit receipt exists | Workers can research good references without either bypassing the gate or becoming unable to record the search | Pending visual work runs dependent implementation, or a pending receipt is treated as verified fit | Active |
